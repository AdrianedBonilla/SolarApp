HELP.md
target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

# Spring Boot
SolarApp/src/main/resources/application.properties

# Docker
# Ignorar archivos y directorios generados por Docker
docker-compose.override.yml
docker-compose.debug.yml
# docker-compose.* # Comentado para permitir subir archivos docker-compose.yml
# Dockerfile.* # Comentado para permitir subir archivos Dockerfile
# **/Dockerfile # Comentado para permitir subir archivos Dockerfile
# **/docker-compose.yml # Comentado para permitir subir archivos docker-compose.yml
# **/docker-compose.yaml # Comentado para permitir subir archivos docker-compose.yaml
# **/docker-compose.override.yml # Comentado para permitir subir archivos docker-compose.override.yml
# **/docker-compose.debug.yml # Comentado para permitir subir archivos docker-compose.debug.yml
# **/docker-compose.* # Comentado para permitir subir archivos docker-compose.*
# **/Dockerfile.* # Comentado para permitir subir archivos Dockerfile.*
# **/docker-compose.yml # Comentado para permitir subir archivos docker-compose.yml
# **/docker-compose.yaml # Comentado para permitir subir archivos docker-compose.yaml

# Ignorar volúmenes de Docker
volumes/
db_data/